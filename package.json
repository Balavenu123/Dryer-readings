{
  "name": "dryer-monitoring",
  "version": "1.0.0",
  "description": "Industrial Dryer Monitoring SCADA System",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
